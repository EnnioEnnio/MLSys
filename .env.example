# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Loaded automatically by `python -m mlsys` (via python-dotenv) for local runs.
# On the cluster, export this in your shell before `sbatch` — .env is not read there.
WANDB_API_KEY=
